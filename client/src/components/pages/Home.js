import React from "react";
import Contacts from "../contact/Contacts";

const Home = () => {
  return (
    <div>
      Welcome to Home PAge
      <Contacts />
    </div>
  );
};

export default Home;
